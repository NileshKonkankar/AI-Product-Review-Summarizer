{
  "name": "ai-product-review-summarizer",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack AI product review summarizer with React, Express, MongoDB, and pluggable AI providers.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace frontend",
    "start:backend": "npm run start --workspace backend",
    "lint": "npm run lint --workspace frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
